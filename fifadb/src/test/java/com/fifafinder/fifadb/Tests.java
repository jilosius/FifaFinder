package com.fifafinder.fifadb;

import org.junit.Test;


public class Tests
{
}
/* angular: provide: alle services und module angeben
            -jasmine für angular
            -mocken immer dann wenn man abhängigkeiten hast
            -createSpyObj zum mocken
            -SpyOn objecte werden verwendet also mock objecte
            -nur für den test wird es gemockt
            -komponente muss deklariert werden die getestet werden soll
            -> compile component -> testen ob es klappt (tobeTruthy?)
            -it blöcke sind tests -> möglichst kleine Tests
            -dialog tests?!
            -Tests sind auch zum dokumentieren
            -httpTestingController ?!

            was testen: ablauflogik, fehlerfälle, edgecases bsp: bei null obwohl parameter, happypath

        @Spy
        @InjectMocks

        @Mocks


        @Test

 */
